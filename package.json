{
  "name": "local-context-engine",
  "version": "0.1.0",
  "description": "Transparent OpenAI-compatible gateway that enforces local LLM context budgets.",
  "license": "Apache-2.0",
  "private": true,
  "type": "module",
  "bin": {
    "local-context-engine": "dist/src/cli.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "check": "npm run typecheck && npm test",
    "test": "tsx --test test/**/*.test.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@lmstudio/sdk": "^1.5.0"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "tsx": "^4.20.0",
    "typescript": "^5.9.0"
  },
  "engines": {
    "node": ">=22"
  }
}
